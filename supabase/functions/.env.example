# Local Edge Function secret only. Use at least 32 random characters.
# Never reuse an application password and never commit the real value.
STEPLINK_CODE_PEPPER=

# SUPABASE_URL and Supabase publishable/secret keys are injected by the platform
# or local Supabase runtime. They are deliberately not duplicated here.
