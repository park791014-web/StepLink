import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'kr.co.steplink.app',
  appName: 'StepLink',
  webDir: 'dist',
  android: {
    backgroundColor: '#f3f1e9',
  },
}

export default config
